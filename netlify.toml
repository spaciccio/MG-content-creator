[build]
  publish = "public"
  command = ""pip install -r public/requirements.txt && python public/main.py"
  functions = "functions"  # Questo indica a Netlify dove trovare le funzioni serverless

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
